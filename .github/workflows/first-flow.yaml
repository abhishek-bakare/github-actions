name: My First Workflow

on: push

jobs:
    first_job:
        name: First job
        runs-on: ubuntu-latest
        steps:
            - name: Welcome message
              run: echo "My first GH Actions job"

            - name: Name files
              run: ls
            
            - name: Read file
              run: cat GITHUB-ACTIONS/README.md