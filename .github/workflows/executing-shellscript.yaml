name: Executing shell script

on: 
    push

jobs:
    ascii_job:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout repo
              uses: actions/checkout@v4
              
            - name: Run shell script
              run: |
                chmod +x my-shell.sh
                ./my-shell.sh